- [ ] Sign-in OTP is always 123456. Need to fix it.
- [x] Add a README.md file for the project.
- [x] Provide an option to choose currency ($ or rupees, etc) when creating an ad listing.
- [x] When viewing an ad listing, the "Report" button should be a secondary action button next to the "Message seller" button. Remove the whole report section below. Instead, move it to a separate modal.
- [x] The search section in the listings page should be minimal. Make it a one-liner.
- [ ] When showing ad listings in the responsive mobile view, the search options should be collapsed/minimized so that not many options are cluttering the GUI.
- [ ] The app should auto-infer the locale and present dates, currencies and number formats accordingly.
- [ ] The application feels very laggy when scrolling. Fix it by either adding smooth scrolling or removing heavy weight components.
- [x] Add list view to the ad listings page (in addition to the current grid view). Then, make it the default view mode.
- [x] As a community admin, in the approval page for community admins, I should be able to see the description, image, price and category of the listing as well. I should be able to update the category just in case the ad poster gave an incorrect one.
- [x] Make the category picker multi-selectable in the ad listings page
- [x] Add Price (Ascending) and Price (Descending) to the sorting options in the products listing page's sort criteria
- [x] As a community admin or super admin, I should be able to "take down" any product listing at any time if I feel it is inappropriate. I should write a mandatory note as to why the product was taken down. And the listing owner should receive an e-mail with the reason and listing detail. The listing owner should see the status & reason of the taken down ad in their "My listings" page.
- [x] When I'm in the desktop browser and zoomed in to 200%, the main body is getting covered at the end by the bottom navbar. Note that the responsive mobile mode is fine and the main body is not covered there.
- [x] In the filter section, show a "Clear all filters" button.
- [x] As a community admin, if I unlist a product, I should have access to see all products and make the unlisted product available again.
- [x] As a listing owner, if my product gets unlisted, I should be able to appeal to the community admin to bring it back up.
- [x] If someone tries to open an unlisted product via its shared link, it should still open, and show all the details of the product but show a warning banner that the product is unlisted/sold, etc and that no further action can be taken upon it.
- [x] Add an image compression library that compresses any image being uploaded by any user. The image should be compressed as much as possible but look good on any laptop or mobile screen without pixelation/blurring.
- [x] Add support for S3 storage alongside local file storage. Add a new property called `storage.type` with a default value "local" to switch between different storage types. Also, add S3 config properties in the same `application.yml` to set the various AWS S3 connection properties. Make sure that the S3 client is vendor-agnostic and supports SeaweedFS as well (without auth for dev testing).
- [x] Add support for Firebase storage alongside local file storage & S3 storage. Also, add Firebase storage config properties in `application.yml`.
- [ ] No direct calls from the browser to the storage service (either S3 or Firebase Storage). The spring boot backend should proxy the calls to these services. Authentication, if any, should be handled on the spring boot backend. This is so that the cloud storage services need not provide a public URL. The spring boot backend will fetch the file content from the storage service using authentication and serve it publicly to the frontend.
